server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;

  location /api/ {
    proxy_set_header Host shop.local;
    proxy_pass http://gateway:8080/;
  }

  location / {
    try_files $uri /index.html;
  }
}
